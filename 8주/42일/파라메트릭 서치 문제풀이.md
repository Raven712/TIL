# 파라메트릭 서치 문제풀이



### 좌표문제류

```python
start = 1 # 거리 차이의 최소값
end = coord[-1] - coord[0] # 거리차이의 최대값. 당연히 좌표 sort
answer = 0


while start <= end:
    mid = (start + end) // 2
    cur = coord[0]				# 현재 좌표와 cnt가 필요하다 (cnt는 요구되는 개수를 넘으면안돼서
    cnt = 1						#								              필요한것)
    for i in coord:
        if i >= cur + mid:
            cnt += 1
            cur = i
    if cnt >= c:
        start = mid + 1
        result = mid        #  조건만족시 결과값에 mid를 넣어줌. 
    else:
        end = mid - 1
print(result)
```

